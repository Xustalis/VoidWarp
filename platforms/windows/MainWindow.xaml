<Window x:Class="VoidWarp.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoidWarp" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4a4e69"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6c63ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel: Device List -->
        <Border Style="{StaticResource Card}" Grid.Column="0" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="VoidWarp" FontSize="24" FontWeight="Bold" Foreground="#6c63ff"/>
                    <TextBlock x:Name="DeviceIdText" Text="Device ID: Loading..." Foreground="#888" FontSize="11" Margin="0,5,0,0"/>
                </StackPanel>
                
                <!-- Discovery Status -->
                <StackPanel Grid.Row="1" Margin="0,15,0,10">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="#888" Margin="0,0,8,0"/>
                        <TextBlock x:Name="StatusText" Text="未开始发现" Foreground="#aaa" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Device List -->
                <ListBox x:Name="DeviceList" Grid.Row="2" 
                         Background="Transparent" BorderThickness="0"
                         Foreground="White" FontSize="13">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding DeviceName}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding IpAddress}" Foreground="#888" FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <!-- Actions -->
                <StackPanel Grid.Row="3" Margin="0,10,0,0">
                    <Button x:Name="DiscoverBtn" Content="开始发现设备" Style="{StaticResource ModernButton}" Click="DiscoverBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Right Panel: Transfer Area -->
        <Border Style="{StaticResource Card}" Grid.Column="1" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header with Receive Mode Toggle -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="文件传输" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="接收模式" Foreground="#888" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <CheckBox x:Name="ReceiveModeToggle" 
                                  Checked="ReceiveModeToggle_Checked" 
                                  Unchecked="ReceiveModeToggle_Unchecked"/>
                    </StackPanel>
                </Grid>
                
                <!-- Receive Status (visible when receiving is enabled) -->
                <Border x:Name="ReceiveStatusPanel" Grid.Row="1" Background="#2a2a4a" CornerRadius="8" 
                        Margin="0,10,0,0" Padding="12" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="ReceiveStatusText" Text="等待接收文件..." Foreground="#6c63ff" FontSize="12"/>
                        <TextBlock x:Name="ReceivePortText" Text="监听端口: -" Foreground="#666" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Incoming Transfer Notification -->
                <Border x:Name="IncomingTransferPanel" Grid.Row="2" Background="#3a3a5a" CornerRadius="12" 
                        Margin="0,15" Padding="20" Visibility="Collapsed" VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="📥 收到文件传输请求" FontSize="16" Foreground="#6c63ff" FontWeight="SemiBold"/>
                        <StackPanel Margin="0,12,0,0">
                            <TextBlock x:Name="IncomingFileName" Text="文件: example.txt" Foreground="White" FontSize="13"/>
                            <TextBlock x:Name="IncomingFileSize" Text="大小: 1.5 MB" Foreground="#aaa" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock x:Name="IncomingSender" Text="来自: Unknown Device" Foreground="#888" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Center">
                            <Button x:Name="AcceptBtn" Content="接受" Style="{StaticResource ModernButton}" 
                                    Background="#4caf50" Margin="0,0,10,0" Click="AcceptBtn_Click"/>
                            <Button x:Name="RejectBtn" Content="拒绝" Style="{StaticResource ModernButton}" 
                                    Background="#f44336" Click="RejectBtn_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Drop Zone (visible when not receiving) -->
                <Border x:Name="DropZone" Grid.Row="2" BorderBrush="#4a4e69" BorderThickness="2" CornerRadius="12" 
                        Margin="0,15" Background="#1a1a2e" 
                        AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center"/>
                        <TextBlock Text="拖拽文件到此处发送" FontSize="16" Foreground="#888" Margin="0,10,0,0"/>
                        <TextBlock Text="先选择目标设备，再拖拽文件" FontSize="12" Foreground="#666"/>
                    </StackPanel>
                </Border>
                
                <!-- Transfer Progress -->
                <StackPanel Grid.Row="3">
                    <TextBlock x:Name="TransferStatus" Text="等待传输..." Foreground="#888" FontSize="12"/>
                    <ProgressBar x:Name="TransferProgress" Height="6" Margin="0,8,0,0" 
                                 Background="#333" Foreground="#6c63ff" Value="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
