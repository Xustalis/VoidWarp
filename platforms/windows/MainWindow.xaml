<Window x:Class="VoidWarp.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoidWarp" 
        Height="750" 
        Width="1150"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        Icon="{StaticResource AppIcon}">
    
    <Window.Resources>
        <!-- ==================== Android-aligned Colors (VoidWarp Theme) ==================== -->
        <Color x:Key="BackgroundColor">#1A1A2E</Color>
        <Color x:Key="SurfaceColor">#16213E</Color>
        <Color x:Key="SurfaceLightColor">#4A4E69</Color>
        <Color x:Key="SurfaceVariantColor">#2A2A4A</Color>
        <Color x:Key="PrimaryColor">#6C63FF</Color>
        <Color x:Key="PrimaryVariantColor">#5A52E0</Color>
        <Color x:Key="SecondaryColor">#FF9800</Color>
        <Color x:Key="SuccessColor">#4CAF50</Color>
        <Color x:Key="WarningColor">#FF9800</Color>
        <Color x:Key="ErrorColor">#FF5252</Color>
        <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
        <Color x:Key="TextSecondaryColor">#B0B0B0</Color>
        <Color x:Key="TextTertiaryColor">#888888</Color>
        <Color x:Key="DividerColor">#333333</Color>
        
        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>
        <SolidColorBrush x:Key="SurfaceVariantBrush" Color="{StaticResource SurfaceVariantColor}"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryVariantBrush" Color="{StaticResource PrimaryVariantColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiaryColor}"/>
        <SolidColorBrush x:Key="DividerBrush" Color="{StaticResource DividerColor}"/>
        
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- VoidWarp logo icon (stylized V) for window and header -->
        <DrawingImage x:Key="AppIcon">
            <DrawingImage.Drawing>
                <GeometryDrawing Brush="{StaticResource PrimaryBrush}" Geometry="M 2,4 L 14,24 L 26,4 L 20,4 L 14,18 L 8,4 Z"/>
            </DrawingImage.Drawing>
        </DrawingImage>
        
        <!-- ==================== Card Style ==================== -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ==================== Modern Button Styles ==================== -->
        <Style x:Key="ModernButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SurfaceVariantBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
        </Style>
        
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        
        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        </Style>
        
        <!-- ==================== Toggle Switch Style ==================== -->
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Track" 
                                Background="{StaticResource SurfaceLightBrush}" 
                                CornerRadius="14" 
                                Padding="2">
                            <Grid>
                                <Ellipse x:Name="Thumb" 
                                         Width="24" Height="24" 
                                         Fill="{StaticResource TextSecondaryBrush}" 
                                         HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ==================== Peer ListBox Item Style ==================== -->
        <Style x:Key="PeerItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" 
                                Background="{StaticResource SurfaceVariantBrush}" 
                                CornerRadius="8" 
                                Padding="12"
                                BorderThickness="2"
                                BorderBrush="Transparent">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ==================== Modern ProgressBar Style ==================== -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <Grid>
                                <Border x:Name="PART_Track" CornerRadius="5"/>
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        CornerRadius="5" 
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20" Background="{StaticResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- ==================== Left Panel: 与安卓一致的发现与设备 ==================== -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header: VoidWarp + 设备ID + 已接收文件 -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Viewbox Width="28" Height="28" Margin="0,0,10,0" VerticalAlignment="Center">
                        <Path Fill="{StaticResource PrimaryBrush}" Data="M 2,4 L 14,24 L 26,4 L 20,4 L 14,18 L 8,4 Z"/>
                    </Viewbox>
                    <TextBlock Text="VoidWarp" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <Border Background="{StaticResource SurfaceVariantBrush}" 
                            CornerRadius="4" 
                            Padding="6,2"
                            HorizontalAlignment="Left"
                            Margin="0,8,0,0">
                        <TextBlock Foreground="{StaticResource TextSecondaryBrush}" 
                                   FontSize="10"
                                   FontWeight="Medium">
                            <Run Text="设备ID: "/>
                            <Run Text="{Binding DeviceId, Mode=OneWay}" 
                                 Foreground="{StaticResource SecondaryBrush}"/>
                        </TextBlock>
                    </Border>
                </StackPanel>
                <Button Grid.Column="1" 
                        Content="已接收文件" 
                        Style="{StaticResource ModernButton}"
                        Padding="12,8"
                        ToolTip="打开已接收文件所在文件夹"
                        Command="{Binding OpenDownloadsCommand}"
                        VerticalAlignment="Center"/>
            </Grid>
            
            <!-- Receive Mode Card (接收准备就绪 / 接收模式已关闭) -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" 
                            Width="40" Height="40" 
                            CornerRadius="20" 
                            Background="{StaticResource SurfaceVariantBrush}" 
                            Margin="0,0,16,0">
                        <TextBlock Text="📄" 
                                   FontSize="18" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ReceiveStatusTitle, Mode=OneWay}" 
                                   FontSize="15"
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding ReceiveStatusSubtitle, Mode=OneWay}" 
                                   FontSize="12" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                    
                    <ToggleButton Grid.Column="2" 
                                  Style="{StaticResource ToggleSwitchStyle}"
                                  IsChecked="{Binding IsReceiving}"
                                  Command="{Binding ToggleReceiverCommand}"/>
                </Grid>
            </Border>
            
            <!-- Diagnostic Info Card (诊断信息) -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16" 
                    Background="{StaticResource SurfaceVariantBrush}" Padding="12">
                <StackPanel>
                    <TextBlock Text="诊断信息" 
                               FontSize="10" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding LocalIpInfo, Mode=OneWay}" 
                               FontSize="11" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               TextWrapping="Wrap"/>
                    <TextBlock Margin="0,4,0,0" FontSize="11" Foreground="{StaticResource PrimaryBrush}">
                        <Run Text="发现: "/>
                        <Run Text="{Binding DiscoveryStatusLabel, Mode=OneWay}"/>
                        <Run Text=" | 监听: "/>
                        <Run Text="{Binding ReceiverPort, Mode=OneWay}"/>
                    </TextBlock>
                    <Button Margin="0,8,0,0" 
                            Content="配置防火墙（若其他设备发现不了本机）" 
                            Style="{StaticResource OutlineButton}"               
                            Padding="12,6"
                            FontSize="11"
                            HorizontalAlignment="Left"
                            Command="{Binding ConfigureFirewallCommand}"
                            ToolTip="以管理员身份运行防火墙规则脚本"/>
                </StackPanel>
            </Border>
            
            <!-- 附近设备 -->
            <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="附近设备" 
                               FontSize="12" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,8"/>
                    
                    <!-- Peers List -->
                    <ListBox Grid.Row="1" 
                             x:Name="PeersListBox"
                             ItemsSource="{Binding Peers}"
                             SelectedItem="{Binding SelectedPeer, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemContainerStyle="{StaticResource PeerItemStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DeviceName}" 
                                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                                   FontWeight="Medium"
                                                   FontSize="14"/>
                                        <TextBlock Text="{Binding DisplayName}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                                   FontSize="12"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                    
                                    <Button Grid.Column="1" 
                                            Content="🔗" 
                                            ToolTip="测试连接"
                                            Padding="8,6"
                                            Background="Transparent"
                                            Foreground="{StaticResource PrimaryBrush}"
                                            BorderThickness="0"
                                            FontSize="14"
                                            Cursor="Hand"
                                            Command="{Binding DataContext.TestConnectionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <!-- Empty State: 未发现设备 + 手动添加 -->
                    <StackPanel Grid.Row="1" 
                                x:Name="EmptyStatePanel"
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Visibility="{Binding HasNoPeers, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="🔗" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                        <TextBlock Text="未发现设备" 
                                   Foreground="{StaticResource TextTertiaryBrush}" 
                                   FontSize="14"
                                   HorizontalAlignment="Center"
                                   Margin="0,12,0,0"/>
                        <Button Content="手动添加" 
                                Style="{StaticResource OutlineButton}"
                                Margin="0,12,0,0"
                                Padding="16,8"
                                Command="{Binding AddManualPeerCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 扫描 + 手动添加 (与安卓底部区域对应，此处放左侧底部) -->
            <StackPanel Grid.Row="4" Margin="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" 
                            Content="{Binding ScanButtonText}" 
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding ScanCommand}"/>
                    <Button Grid.Column="2" 
                            Content="+ 添加" 
                            Style="{StaticResource ModernButton}"
                            Command="{Binding AddManualPeerCommand}"/>
                </Grid>
            </StackPanel>
        </Grid>
        
        <!-- ==================== Right Panel: 发送文件 + 传输状态 + 日志 ==================== -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Send File Card (选择要发送的文件 + 开始传送) -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16"
                    Background="{StaticResource SurfaceVariantBrush}">
                <StackPanel>
                    <!-- File Selector -->
                    <Border Background="{StaticResource BackgroundBrush}" 
                            CornerRadius="12" 
                            Padding="16"
                            Margin="0,0,0,16"
                            Cursor="Hand"
                            MouseLeftButtonDown="FileSelectArea_MouseLeftButtonDown">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="📄" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding SelectedFileInfo, Mode=OneWay}" 
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       FontSize="13"/>
                        </Grid>
                    </Border>
                    
                    <!-- Buttons: 扫描/停止 + 开始传送 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" 
                                Content="{Binding ScanButtonText}" 
                                Style="{StaticResource ModernButton}"
                                Height="50"
                                Command="{Binding ScanCommand}"/>
                        <Button Grid.Column="2" 
                                Style="{StaticResource PrimaryButton}"
                                Height="50"
                                Command="{Binding SendFileCommand}"
                                IsEnabled="{Binding CanSend}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🚀" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="开始传送" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- Transfer Status Card (传输状态) -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="传输状态" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Margin="0,0,0,12"/>
                    
                    <TextBlock Grid.Row="1" 
                               Text="{Binding StatusMessage}" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="14"
                               Margin="0,0,0,16"/>
                    
                    <ProgressBar Grid.Row="2" 
                                 Style="{StaticResource ModernProgressBar}"
                                 Value="{Binding ProgressValue}" 
                                 Maximum="100"
                                 Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="3" 
                               HorizontalAlignment="Right"
                               Foreground="{StaticResource PrimaryBrush}"
                               FontSize="13"
                               FontWeight="SemiBold">
                        <Run Text="{Binding ProgressValue, StringFormat={}{0:F1}}"/>
                        <Run Text="%"/>
                    </TextBlock>
                </Grid>
            </Border>
            
            <!-- Log Console (日志) -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="日志" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    
                    <Border Grid.Row="1" 
                            Background="#0D1117" 
                            CornerRadius="8" 
                            Padding="12"
                            BorderThickness="1"
                            BorderBrush="#21262D">
                        <ScrollViewer x:Name="LogScrollViewer" 
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding Logs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" 
                                                   Foreground="#8B949E" 
                                                   FontSize="12" 
                                                   FontFamily="Consolas, Courier New, monospace"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                    
                    <Grid Grid.Row="2" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding LocalIpInfo}"
                                   Foreground="{StaticResource TextTertiaryBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                        <Button Grid.Column="1" 
                                Content="清空" 
                                Style="{StaticResource ModernButton}"
                                Padding="16,8"
                                FontSize="12"
                                Command="{Binding ClearLogsCommand}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
